<form method="post" action="{{ path('operations_delete', {'id': category_choice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_choice.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
