{% extends 'nav.base.html.twig' %}

{% set projectslink = 'active' %}

{% block title %}New Project{% endblock %}

{% block content %}
	<div class="mt-3 p-3 container-xl bg-white shadow-sm">
		<h1>Create new Project</h1>

		{{ include('projects/_form.html.twig') }}

		<a class="mt-1 btn btn-secondary" href="{{ path('project_index') }}">back to list</a>
	</div>
{% endblock %}
